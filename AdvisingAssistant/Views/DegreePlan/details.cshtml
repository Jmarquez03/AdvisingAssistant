@model DegreePlan

<h2>@Model.Major Degree Plan</h2>
<table class="table">
    <thead>
        <tr>
            <th>Course Name</th>
            <th>Course Code</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var course in Model.Courses)
        {
            <tr>
                <td>@course.Name</td>
                <td>@course.CourseNumber</td>
            </tr>
        }
    </tbody>
</table>
